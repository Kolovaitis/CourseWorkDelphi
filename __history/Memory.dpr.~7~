program Memory;

uses
  System.StartUpCopy,
  FMX.Forms,
  Main in 'Main.pas' {FormMain},
  Logic in 'Logic.pas',
  MyCardView in 'MyCardView.pas',
  StartMenu in 'StartMenu.pas' {Form1};

{$R *.res}

begin
   Application.Initialize;
   Application.CreateForm(TForm1, Form1);
  //Application.CreateForm(TFormMain, FormMain);

  Application.Run;

end.
